services:
  db:
    image: postgres:16
    container_name: controlrecetamedica_db
    environment:
      POSTGRES_DB: controlrecetamedica
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app123
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: